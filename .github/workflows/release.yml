name: Compile Release
on:
  push:
    tags:
      - 'v*'

jobs:
  build-release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - uses: actions/cache@v4
        with:
          path: |
            ~/.cache/pip
            ~/.platformio/.cache
          key: ${{ runner.os }}-pio

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install PlatformIO Core
        run: pip install --upgrade platformio

      - name: Build CrossPoint
        run: pio run -e gh_release

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          name: CrossPoint Reader CJK ${{ github.ref_name }}
          body: |
            ## CrossPoint Reader CJK ${{ github.ref_name }}

            基于 [CrossPoint Reader](https://github.com/daveallie/crosspoint-reader) 的 CJK 适配版本。

            ### ✨ 主要功能

            - **深色模式支持**: 系统级深色模式，E-ink 无闪烁切换
              - 所有 UI 元素自动像素反转
              - 优化刷新时序，防止白屏闪烁
              - 章节切换流畅无感

            - **CJK 字体支持**: 增强的中日韩文本渲染
              - 外置字体管理系统
              - UI 文本国际化
              - 多语言界面支持 (中文/英文/日语)

            ### 🔧 技术改进

            - Fork 并集成 community-sdk，便于维护
            - 修复灰度还原时序，消除视觉伪影
            - 自动化 GitHub release 创建流程

            ### 📦 SDK 版本

            - community-sdk: v0.1.0-cjk (基于上游 fe766f1)

            ### 📥 安装方式

            1. 下载 `firmware.bin`
            2. 访问 https://xteink.dve.al/ 使用 OTA 刷入
            3. 或使用 esptool.py 手动刷入：
               ```bash
               esptool.py --chip esp32c3 write_flash 0x0 bootloader.bin 0x8000 partitions.bin 0x10000 firmware.bin
               ```

            ### 📝 详细说明

            - [README](https://github.com/${{ github.repository }})
            - [完整变更日志](https://github.com/${{ github.repository }}/commits/${{ github.ref_name }})
          files: |
            .pio/build/gh_release/firmware.bin
            .pio/build/gh_release/bootloader.bin
            .pio/build/gh_release/partitions.bin
          draft: false
          prerelease: false
